import React from 'react';
import './Header.css'

const Header = () => (
    <div id='head'>
    Thumbnails
      <div id='instructions'>
        Choose from the thumbnails below to view the full image and add a description.
      </div>
    </div>
  );
  
  export default Header;